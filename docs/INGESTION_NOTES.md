# Ingestion Notes

SAF works with files stored under `SAF_DATA_ROOT`. Use paths relative to that directory when calling tools.

Supported input formats:

- `.sav`
- `.zsav`
- `.por`
- `.csv`
- `.tsv`

Do not commit real survey datasets or respondent records to this repository. Keep sensitive data in a local, access-controlled directory outside git.
