# Roadmap

- Add richer variable-label and value-label summaries.
- Add optional missing-value rule summaries from file metadata.
- Add export support for labeled CSV dictionaries.
- Add clearer profiling for dates and categorical variables.
- Add fixtures built from synthetic datasets for broader tests.
- Add optional streamable HTTP deployment notes once MCP package support is stable.
